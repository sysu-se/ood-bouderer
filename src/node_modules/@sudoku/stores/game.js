import { gameSession, invalidCells } from './gameSession';
import { SUDOKU_SIZE } from '@sudoku/constants';
import { derived, writable } from 'svelte/store';

export const gamePaused = writable(true);

export const gameWon = derived(
	[gameSession, invalidCells],
	([$activeGrid, $invalidCells]) => {
		for (let row = 0; row < SUDOKU_SIZE; row++) {
			for (let col = 0; col < SUDOKU_SIZE; col++) {
				if ($activeGrid[row][col] === 0) return false;
			}
		}

		return $invalidCells.length === 0;
	},
	false,
);
